- After a commit to github run the following command
  git push heroku main

- Then restart the app
  heroku restart -a sasa-business-listing-app

- After that you can view logs using
  heroku logs --tail
